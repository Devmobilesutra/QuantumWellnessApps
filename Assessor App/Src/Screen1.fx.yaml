Screen1 As screen:

    Video1 As videoPlayback:
        BorderColor: =RGBA(149, 149, 149, 1)
        BorderThickness: =2
        Fill: =RGBA(0, 0, 0, 0)
        Height: =382
        Media: =First(UploadedVideos).Value
        Width: =423
        X: =56
        Y: =110
        ZIndex: =1

    AddMediaWithImage3 As group:
        Height: =5
        Width: =5
        X: =40
        Y: =40
        ZIndex: =3

        UploadedImage3 As image:
            BorderColor: =RGBA(255, 0, 0, 1)
            BorderThickness: =2
            Height: =0
            Image: =If(IsBlank(AddMediaButton2.Media), SampleImage, AddMediaButton2.Media)
            ImagePosition: =ImagePosition.Fill
            Width: =0
            X: =AddMediaButton2.X
            Y: =541
            ZIndex: =4

        AddMediaButton2 As addMedia:
            BorderColor: =RGBA(255, 0, 0, 1)
            BorderThickness: =2
            ChangePictureText: ="Change Video"
            Height: =81
            OnChange: =  ClearCollect(UploadedVideos, AddMediaButton2.Media)
            Size: =25
            Text: ="Tap or click to add a Video"
            Width: =500
            X: =42
            Y: =527
            ZIndex: =5

    Button5 As button:
        Height: =85
        OnSelect: |-
            =
            Collect(
                            GC_checkpointItem_Images,
                            {
                                 checkpointItemImagesId:"cb8d59de-2d3f-409a-a677-8bb3797625d1",
                                
                                /*checkpointItemImagesId: varBankUnitId & "cii" & Text(
                                    Now(),
                                    "mmddyyyyhhmmss"
                                ) & "-" & Rand(),*/
                                assignmentId: LookUp(
                                    DropColumns(
                                        gcProjectAssignment,
                                        AstatusUpdate
                                    ),
                                    Id = varAssignmentId
                                ),
                                //  bankId:LookUp(gcBank,Id=VarBankId),
                                bankUnitId: LookUp(
                                    gcBankunit,
                                    Id = varBankUnitId
                                ),
                                section_id: LookUp(
                                    DropColumns(
                                        GcSectionAssessment,
                                        SstatusUpdate
                                    ),
                                    'sectionAssessment (quantum_sectionassessment)' = varSA
                                ),
                                checkpoint_id: LookUp(
                                    DropColumns(
                                        GcCheckpointAssessment,
                                        CstatusUpdate
                                    ),
                                    checkpointAssessment_id = GuidId
                                ),
                                checkpointItem_Id: LookUp(
                                    DropColumns(
                                        GC_checkpointItemAssessment,
                                        CIstatusUpdate
                                    ),
                                    'checkpointItemAssessment (quantum_checkpointitemassessment)' = Text(varCItemA)
                                ),
                                product_id: LookUp(
                                    GC_product,
                                    product_Model = glryUnitBS.Selected.productModel
                                ),
                                image_annotation: "",
                               // Image:Video1.Image,
                                ImageSortSequence: LookUp(
                                    GC_templateSections,
                                    sectionId = VarSectionId,
                                    sectionSequence
                                ) & LookUp(
                                    GC_templateCheckpoints,
                                    checkpointId = ChId,
                                    checkpointSequence
                                ) & LookUp(
                                    GC_templateCheckpointsItem,
                                    checkpointItemId = varCheckpointitemID,
                                    checkpointItemSequence
                                ),
                                CIIstatusUpdate: Blank()
                                
                            }
                        );
            
                        UpdateIf(GC_checkpointItem_Images,checkpointItemImagesId="cb8d59de-2d3f-409a-a677-8bb3797625d1",{Image:Video1.Image});
        Text: ="Button"
        Width: =247
        X: =926
        Y: =130
        ZIndex: =6

    Video2 As videoPlayback:
        Media: |
            =First(GC_checkpointItem_Images).Image.media
        X: =742
        Y: =320
        ZIndex: =7

